
@using ISProject.Models
@model HeaderIADCLS

<div>
    <h1 class="right">IAD-03</h1>
    <h1 class="right">INFORME DE ACTIVIDADES Y DOCENCIA</h1>
    <h1 class="right">INFORME DE ACTIVIDADES Y DOCENCIA SEMESTRE: @Model.periodo</h1>
    <br><br>
    <table>
        <tr>
            <td> Nombre del academico:</td>
            <td colspan="3"> @Model.nombre</td>
            <td> No. de Empleado</td>
            <td> @Model.numero_empleado</td>
        </tr>
        <tr>
            <td> Categoría:</td>
            <td> @Model.categoria</td>
            <td> Horas de clase:</td>
            <td> @Model.horas_clase horas</td>
            <td> Horas de gestión:</td>
            <td> @Model.horas_gestion horas</td>
        </tr>
        <tr>
            <td> Cargo:</td>
            @using (Ajax.BeginForm("ChangeCargo", null, new AjaxOptions
            {
                HttpMethod = "POST"
            }, new { id = "frmChangeCargo" }))
            {
                <td> @Html.DropDownList("id_cargo", Model.cargos)</td>
                @Html.Hidden("id_iad", Model.id_iad)
            }
            <td>Horas de investigación:</td>
            <td style="width:80px;"> @Model.horas_investigacion horas</td>
            <td>Horas de tutorías o dirección de trabajos:</td>
            <td style="width:80px;"> @Model.horas_tutorias horas</td>
        </tr>
    </table> <br><br>
</div>
<script>
    document.getElementById("id_cargo").onchange = function () {
        $("#frmChangeCargo").submit();
    }
</script>
